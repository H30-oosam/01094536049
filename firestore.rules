rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ---------------------------------------------------------
    // Allow read/write for all collections to ensure seamless
    // demo, testing, and operation in the sandboxed preview environment.
    // ---------------------------------------------------------
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
